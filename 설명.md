Windows PC
│
├─ VS Code
├─ Git
│
└─ Oracle VM
   └─ Ubuntu
      ├─ Docker
      ├─ Spring Boot 실행
      └─ GitHub Actions Self-hosted Runner
                ▲
                │ 인터넷
                ▼
             GitHub
                ▲
                │ git push
                │
             개발자

Spring Boot 프로젝트
│
├── Dockerfile
│
├── docker-compose.yml
│
└── .github
    └── workflows
        └── deploy.yml


| 파일                   | 역할                               |
| -------------------- | -------------------------------- |
| `Dockerfile`         | Spring Boot Docker 이미지 만드는 방법    |
| `docker-compose.yml` | Docker 컨테이너 실행 방법                |
| `deploy.yml`         | GitHub push부터 자동 배포까지            |
| Self-hosted Runner   | `deploy.yml`을 실제로 실행하는 Ubuntu VM |
